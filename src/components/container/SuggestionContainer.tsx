import React from "react";
import NewBooks from "../newBooks";

const SuggestionContainer = () => {
  return (
    <div id="book-suggestions" className="mx-auto mb-6 sm:w-[40%] w-[80%]">
      <NewBooks />
    </div>
  );
};

export default SuggestionContainer;
